class Solution {
public:
    bool isPowerOfThree(int n) {
        bool result = false;
        int i =0;
        while(n>=pow(3, i)){
            if(pow(3, i)<n){
                i++;
            }else if(pow(3, i)== n){
                return true;
            }
        }
        return result;
    }
};
